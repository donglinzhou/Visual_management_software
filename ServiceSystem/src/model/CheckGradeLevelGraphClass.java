package model;

public class CheckGradeLevelGraphClass implements CheckGradeLevelGraphBehavior {
	//简单想了一下，传回的一个二维的ArrayList数组
	//一个元组有学期名，所有考试总成绩？这个不大可能实现，各个科目的最高分，我都不知道咋传回去，平均分的历史波动？
	//一个二维的数组解决不了，这边建议调用前面的函数，去分时间段读取，这个不大可能直接实现。
}
