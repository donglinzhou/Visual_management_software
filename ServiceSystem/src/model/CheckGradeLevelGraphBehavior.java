package model;

public interface CheckGradeLevelGraphBehavior {
	//分数特征升降图
	//更加理解不了，太抽象了
	//能想到的解决方式有点复杂
	//首先通过教师ID和学生ID去找到有关的信息，然后读出来，做一个计算，这功能太离谱了
	//推荐从数据库下手
	//如果可以通过数据库去建立总成绩表，这个可以通过程序去写，但数据库得有表，好像也可以直接
	//算了，太麻烦了，总体上先不动
	
}
